spring:
  application:
    name: reconpay
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          batch_size: 100
          time_zone: UTC
        order_inserts: true
        order_updates: true
  flyway:
    enabled: true

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION:86400}

management:
  endpoints:
    web:
      exposure:
        # Only /actuator/health is public; everything else here still requires
        # an authenticated request through the security filter chain.
        include: health,info,prometheus,metrics
  endpoint:
    health:
      show-details: never
  metrics:
    tags:
      application: ${spring.application.name}
  tracing:
    sampling:
      probability: ${TRACING_SAMPLE_RATE:0.1}

reconpay:
  reconciliation:
    amount-tolerance: ${RECONCILIATION_AMOUNT_TOLERANCE:0.00}
    settlement-lag-days: ${RECONCILIATION_SETTLEMENT_LAG_DAYS:5}
    max-window-days: ${RECONCILIATION_MAX_WINDOW_DAYS:366}
    async: ${RECONCILIATION_ASYNC:true}
    workers: ${RECONCILIATION_WORKERS:2}
    queue-capacity: ${RECONCILIATION_QUEUE_CAPACITY:50}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
  default-flat-param-object: true
